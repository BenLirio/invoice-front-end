'use strict'
import _ from 'lodash'
export default class ModelBuilder {
  constructor(data) {
    this.build(data)
  }
  build(data) {
    for (const key in data) {
      if (data[key] !== null && )
    }
  }
}
